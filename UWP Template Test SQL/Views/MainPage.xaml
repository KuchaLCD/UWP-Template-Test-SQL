<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:Microsoft.UI.Xaml.Controls"
    x:Class="UWP_Template_Test_SQL.Views.MainPage"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <!-- 
    <Page.Resources>
        -Кастомный шаблон для TabViewItem-
        <ControlTemplate x:Key="CustomTabViewItemTemplate" TargetType="Custom:TabViewItem">
            <Grid x:Name="RootGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                -Заголовок TabViewItem-
                <TextBlock
                    Text="{TemplateBinding Header}"
                    VerticalAlignment="Center"
                    Margin="10,0,0,0"/>

                -Кнопка "+" вместо крестика-
                <Button
                    x:Name="AddButton"
                    Grid.Column="1"
                    Content="+"
                    Width="30"
                    Height="30"
                    Margin="5"
                    Click="AddButton_Click"/>
            </Grid>
        </ControlTemplate>
    </Page.Resources>
    -->
    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <Pivot Title="Pivot">
                <PivotItem Header="PivotItem 0">
                    <Grid>
                        <Custom:DropDownButton Content="DropDownButton" Margin="277,191,0,0" VerticalAlignment="Top"/>
                        <Custom:SplitButton Content="SplitButton" Margin="519,191,0,0" VerticalAlignment="Top"/>
                        <Custom:ToggleSplitButton Content="ToggleSplitButton" Margin="693,191,0,0" VerticalAlignment="Top"/>
                        <Custom:RatingControl HorizontalAlignment="Left" Margin="639,303,0,0" VerticalAlignment="Top"/>
                        <AppBarButton Icon="Accept" Label="AppBarButton" Margin="787,428,0,0"/>
                        <ToolTip Content="ToolTip" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
                    </Grid>
                </PivotItem>
                <PivotItem Header="PivotItem 1">
                    
                </PivotItem>
            </Pivot>
        </Grid>
    </Grid>
</Page>
